import React from 'react';
import PropTypes from 'prop-types';

function Span({
  value,
  id,
  classIdentifier,
}) {
  return ();
};

Span.propTypes = {
  value: PropTypes.string.isRequired,
};

Span.defaultProps = {
  id: '',
  classIdentifier: '',
};

export default Span;
